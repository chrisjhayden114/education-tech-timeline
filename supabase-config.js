// Empty placeholders. On Netlify, scripts/write-supabase-config.js overwrites this
// from SUPABASE_URL and SUPABASE_ANON_KEY so comments are shared across all readers.
// Until those env vars are set, comments stay in each browser's localStorage (demo mode).
window.SUPABASE_URL = '';
window.SUPABASE_ANON_KEY = '';
